import { LocalizedDecimalPipe } from './localized-decimal.pipe';

describe('LocalizedDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizedDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
